// Circumference of a Circle Javascript

function circum(r) {
    const pi = 3.14
    let circumference = 2 * pi * r
    return circumference
}

result = circum(5);
console.log("circumference of the circle is"  + result)